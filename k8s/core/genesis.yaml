---
apiVersion: v1
kind: Service
metadata:
  name: core-genesis
  labels:
    app.kubernetes.io/name: core-genesis
spec:
  clusterIP: None
  ports:
    - name: p2p
      port: 26656
      protocol: TCP
      targetPort: 26656
    - name: rpc
      port: 26657
      protocol: TCP
      targetPort: 26657
    - name: http
      port: 8081
      protocol: TCP
      targetPort: 8081
    - name: grpc
      port: 9090
      protocol: TCP
      targetPort: 9090
    - name: rest
      port: 1317
      protocol: TCP
      targetPort: 1317
  selector:
    app.kubernetes.io/name: core-genesis
---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: core-genesis
spec:
  serviceName: core-genesis
  replicas: 1
  revisionHistoryLimit: 3
  selector:
    matchLabels:
      app.kubernetes.io/instance: core
      app.kubernetes.io/name: core-genesis
  template:
    metadata:
      annotations:
        quality: release
        role: api-gateway
        sla: high
        tier: gateway
      labels:
        app.kubernetes.io/instance: core
        app.kubernetes.io/type: core-validator
        app.kubernetes.io/name: core-genesis
        app.kubernetes.io/version: '0.1'
    spec:
      imagePullSecrets:
        - name: test-docker-creds
      initContainers:
        - name: init-genesis
          image: persistenceone/core
          imagePullPolicy: Always
          env:
            - name: COINS
              value: "100000000000000000uxprt"
            - name: CHAIN_ID
              valueFrom:
                configMapKeyRef:
                  key: chain.id
                  name: core
            - name: CORE_DIR
              value: /persistencecore
          command:
            - bash
            - "-c"
            - |
              VAL_INDEX=${HOSTNAME##*-}
              echo "Validator Index: $VAL_INDEX"
              
              persistencecore init core --chain-id $CHAIN_ID --home $CORE_DIR
              jq -r ".genesis[0].mnemonic" /configs/validators.json | persistencecore keys add $(jq -r ".genesis[0].name" /configs/validators.json) --recover --keyring-backend='test' --home $CORE_DIR
              
              # Provide genesis validator self deligations
              persistencecore add-genesis-account $(jq -r .genesis[$i].address /configs/validators.json) $COINS --keyring-backend='test' --home $CORE_DIR
              
              # Give Validator addresses initial coins
              for ((i=0; i<$(jq -r '.validators | length' /configs/validators.json); i++))
              do
                jq -r ".validators[$i].mnemonic" /configs/validators.json | persistencecore keys add $(jq -r ".validators[$i].name" /configs/validators.json) --recover --keyring-backend='test' --home $CORE_DIR
                persistencecore add-genesis-account $(jq -r .validators[$i].address /configs/validators.json) $COINS --keyring-backend='test' --home $CORE_DIR
              done
              
              for ((i=0; i<$(jq -r '.keys | length' /configs/keys.json); i++))
              do
                jq -r ".keys[$i].mnemonic" /configs/orchestrator.json | persistencecore keys add $(jq -r ".keys[$i].name" /configs/orchestrator.json) --recover --keyring-backend='test' --home $CORE_DIR
              done
              
              persistencecore gentx $(jq -r ".genesis[0].name" /configs/validators.json) 5000000000uxprt --keyring-backend="test" --chain-id $CHAIN_ID --home $CORE_DIR
              echo "Output of gentx"
              cat $CORE_DIR/config/gentx/*.json | jq
              
              echo "Running collect-gentxs"
              persistencecore collect-gentxs --home $CORE_DIR
              
              sed -i 's#"tcp://127.0.0.1:26657"#"tcp://0.0.0.0:26657"#g' $CORE_DIR/config/config.toml
              sed -i 's/timeout_commit = "5s"/timeout_commit = "1s"/g' $CORE_DIR/config/config.toml
              sed -i 's/timeout_propose = "3s"/timeout_propose = "1s"/g' $CORE_DIR/config/config.toml
              sed -i 's/index_all_keys = false/index_all_keys = true/g' $CORE_DIR/config/config.toml
              sed -i 's/unsafe = false/unsafe = true/g' $CORE_DIR/config/config.toml
              sed -i 's/minimum-gas-prices = ""/minimum-gas-prices = "0.00uxprt"/g' $CORE_DIR/config/config.toml
              sed -i 's/cors_allowed_origins = []/cors_allowed_origins = ["*"]/g' $CORE_DIR/config/config.toml
              
              sed -i 's/enable = false/enable = true/g' $CORE_DIR/config/app.toml
              sed -i 's/swagger = false/swagger = true/g' $CORE_DIR/config/app.toml
              sed -i 's/enabled-unsafe-cors = false/enabled-unsafe-cors = true/g' $CORE_DIR/config/app.toml
              
              jq '.consensus_params.block.max_bytes |= "42020096"' $CORE_DIR/config/genesis.json | tee $CORE_DIR/config/genesis.new.json
              mv $CORE_DIR/config/genesis.new.json $CORE_DIR/config/genesis.json
              jq '.app_state.gov.voting_params.voting_period |= "30s"' $CORE_DIR/config/genesis.json | tee $CORE_DIR/config/genesis.new.json
              mv $CORE_DIR/config/genesis.new.json $CORE_DIR/config/genesis.json
              jq '.app_state.gov.tally_params.quorum |= "0.0"' $CORE_DIR/config/genesis.json | tee $CORE_DIR/config/genesis.new.json
              mv $CORE_DIR/config/genesis.new.json $CORE_DIR/config/genesis.json
              jq '.app_state.gov.deposit_params.min_deposit[0].amount |= "10"' $CORE_DIR/config/genesis.json | tee $CORE_DIR/config/genesis.new.json
              mv $CORE_DIR/config/genesis.new.json $CORE_DIR/config/genesis.json
              
              jq '.app_state.crisis.constant_fee.denom |= "uxprt"' $CORE_DIR/config/genesis.json | tee $CORE_DIR/config/genesis.new.json
              mv $CORE_DIR/config/genesis.new.json $CORE_DIR/config/genesis.json
              jq '.app_state.gov.deposit_params.min_deposit[0].denom |= "uxprt"' $CORE_DIR/config/genesis.json | tee $CORE_DIR/config/genesis.new.json
              mv $CORE_DIR/config/genesis.new.json $CORE_DIR/config/genesis.json
              jq '.app_state.liquidity.params.pool_creation_fee[0].denom |= "uxprt"' $CORE_DIR/config/genesis.json | tee $CORE_DIR/config/genesis.new.json
              mv $CORE_DIR/config/genesis.new.json $CORE_DIR/config/genesis.json
              jq '.app_state.mint.params.mint_denom |= "uxprt"' $CORE_DIR/config/genesis.json | tee $CORE_DIR/config/genesis.new.json
              mv $CORE_DIR/config/genesis.new.json $CORE_DIR/config/genesis.json
              jq '.app_state.staking.params.bond_denom |= "uxprt"' $CORE_DIR/config/genesis.json | tee $CORE_DIR/config/genesis.new.json
              mv $CORE_DIR/config/genesis.new.json $CORE_DIR/config/genesis.json
              
              echo "TRYING TO PRINT GENESIS JSON"
              cat $CORE_DIR/config/genesis.json | jq
              
              
              persistencecore tendermint show-node-id --home $CORE_DIR > /persistencecore/genesis-node-id.txt
          resources:
            limits:
              cpu: "0.2"
              memory: "200M"
            requests:
              cpu: "0.1"
              memory: "100M"
          volumeMounts:
            - mountPath: /persistencecore
              name: persistencecore
            - mountPath: /configs
              name: addresses
      containers:
        - name: validator
          image: persistenceone/core
          imagePullPolicy: Always
          env:
            - name: CHAIN_ID
              valueFrom:
                configMapKeyRef:
                  key: chain.id
                  name: core
            - name: CORE_DIR
              value: /persistencecore
          command:
            - bash
            - "-c"
            - |
              persistencecore start --home $CORE_DIR
          resources:
            limits:
              cpu: "1"
              memory: "1G"
            requests:
              cpu: "0.5"
              memory: "500M"
          volumeMounts:
            - mountPath: /persistencecore
              name: persistencecore
            - mountPath: /configs
              name: addresses
        - name: exposer-app
          image: persistenceone/exposer:core
          imagePullPolicy: Always
          env:
            - name: HOME_DIR
              value: /persistencecore
            - name: GENESIS_FILE
              value: /persistencecore/config/genesis.json
            - name: PORT
              value: "8081"
            - name: CLIENT_NAME
              value: "persistencecore"
          command: [ "exposer" ]
          resources:
            limits:
              cpu: "0.1"
              memory: "100M"
            requests:
              cpu: "0.1"
              memory: "100M"
          volumeMounts:
            - mountPath: /persistencecore
              name: persistencecore
      volumes:
        - name: persistencecore
          emptyDir: { }
        - name: addresses
          configMap:
            name: test-addresses-core
---
